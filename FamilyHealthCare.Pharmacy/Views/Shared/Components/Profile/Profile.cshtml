@using Contract.DTOs.ManagementService
@using FamilyHealthCare.SharedLibrary
@model PharmacyDetailsDto
@{
}
<li class="nav-item dropdown has-arrow">
    <a href="#" class="dropdown-toggle nav-link" data-bs-toggle="dropdown">
        <span class="user-img"><img class="rounded-circle" src=@(ImageConstants.PHARMACIES+Model.Avatar) width="31" alt="@Model.PharmacyName"></span>
    </a>
    <div class="dropdown-menu">
        <div class="user-header">
            <div class="avatar avatar-sm">
                <img src=@(ImageConstants.PHARMACIES+Model.Avatar) alt="User Image" class="avatar-img rounded-circle">
            </div>
            <div class="user-text">
                <h6>@Model.PharmacyName</h6>
                <p class="text-muted mb-0">Pharmacy</p>
            </div>
        </div>
        <a class="dropdown-item" asp-controller="Account" asp-action="ProfileSetting">My Profile</a>
        <a class="dropdown-item" asp-controller="Account" asp-action="SignOut">Logout</a>
    </div>
</li>